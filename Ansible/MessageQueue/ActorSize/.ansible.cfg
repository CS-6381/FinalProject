
[defaults]

inventory = ~/.ansible/MyInventory

intepreter_python=/usr/bin/python3
    
private_key_file = ~/.ssh/redis-test.pem

host_key_checking = False

allow_world_readable_tmpfiles = True

timeout = 200
